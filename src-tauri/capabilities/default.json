{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for every app window — `FR-UI-9`: 'Open in new window' creates additional `workspace-*`-labelled windows from the same frontend bundle, which need the same permission set as `main`.",
  "windows": ["main", "workspace-*"],
  "permissions": [
    "core:default",
    "core:window:allow-create",
    "core:webview:allow-create-webview-window",
    "opener:default",
    "log:default",
    "dialog:default",
    "updater:default",
    "process:allow-restart"
  ]
}
