# Generated by Cargo
# will have compiled files and executables
/target/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas

# Stray per-type output from ts-rs's own #[ts(export)] mechanism, if it's ever
# reintroduced accidentally — src/lib/bindings.ts (via `npm run gen:bindings`) is the only
# sanctioned bindings output (see src-tauri/src/bin/export_bindings.rs).
/bindings
